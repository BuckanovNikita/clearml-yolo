# Release 0.3.0 verification summary — 2026-09-20

This records the original release verification completed before the subsequent
machine-environment cleanup. It is not a claim that later changes reran live training.

- 325 tests passed; Ruff, strict mypy, seven import contracts, pre-commit, shell syntax,
  whitespace and local documentation checks passed.
- Real CPU and single-GPU pipelines exercised native YAML and embedded mappings,
  no-baseline behavior, automatic production-baseline selection, current-test
  re-inference, and developer/business reports.
- Standalone validation, comparison, report generation and nondefault evaluation
  options completed. Paired counts, inference settings, image membership and frozen
  thresholds agreed across the evaluated results.
- Seven accepted workflows yielded 391 force-downloaded artifact files, each hashed.
  Failed attempts and deliberate upload/interruption failures were recorded separately.
  Exactly one task belonged to each invocation; no native duplicate models appeared.
- Wheel and source archive installed in separate clean environments. All eight
  commands were available, removed commands were absent, and native Albumentations
  transforms remained available.
- Task-owned resources were cleaned up. A fresh independent review returned `ship`.
  All 25 release tasks were completed.

Physical multi-GPU execution remains unverified. The synthetic fixture verifies
workflow behavior, not production model quality.

Machine-specific commands, task identities, endpoints, hardware observations and
artifact hashes were preserved byte-for-byte in the global `clearml-yolo-environment`
skill under `references/release-030-evidence/`, with a SHA-256 migration manifest.
The archive is historical; use that skill's current workflow for local operations.
Other environments should record their own execution evidence.
